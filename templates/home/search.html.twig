{% extends 'base.html.twig' %}

{% block title %}Recherche{% endblock %}

{% block body %}
    <div class="search-container">
        <form>
            <label>Search :
                <input type="search" placeholder="Que cherchez-vous ?" name="annonce">
            </label>
            <label>
                <select class="categorie-selector" name="categorie">
                    <option value="">Choisissez une catégorie</option>
                    {% for categorie in categorie_list %}
                        <option value="{{ categorie.id }}">{{ categorie.nom }}</option>
                    {% endfor %}
                </select>
            </label>
            <button type="submit">Entrer</button>
        </form>
    </div>

    <div class="result-container">
        {% if resultats %}
            {% for produit in resultats %}
                <div>
                    <h4>{{ produit.nom }}</h4>
                    <a href="{{ path('app_produit_id', {'id': produit.id}) }}">Voir l'annonce</a>
                </div>
            {% endfor %}
        {% else %}
            <h3>Désolé, nous n'avons rien qui correspond à votre recherche 😌</h3>
        {% endif %}
    </div>
{% endblock %}

